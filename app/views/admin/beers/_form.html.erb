<%= form_with(model: beer, url: admin_beers_path) do |form| %>
  <% if beer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(beer.errors.count, "error") %> prohibited this beer from being saved:</h2>

      <ul>
      <% beer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :beer_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :beer_description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :beer_price %>
  </div>

  <div class="field">
    <%= form.label :opened %>
    <%= form.datetime_select :opened, id: :beer_opened %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
